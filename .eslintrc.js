// Used to configure ESLint also known as Ecma script for React Native projects.

module.exports = {
  root: true,
  extends: '@react-native',
};

// Android and ios folders are used to compile the builds for the respective platforms.
// The android folder contains the source code for the Android app.
// The ios folder contains the source code for the iOS app.
// The node_modules folder contains the dependencies of the project.
// The App.js file is the main entry point of the app. It is where the components get registered and the app gets started.
// The package.json file contains the metadata of the project, such as the name, version, and dependencies.
// The babel.config.js file helps in converting the code into the latest and most efficient version of the code.
// The metro.config.js file helps in fast reloading the app when the code changes. It is used by the Metro bundler to build the app.
// The .eslintrc.js file is used to configure ESLint for React Native projects.
// The index.js file is the main entry point of the javascript file. Inside which the components get registered and the app gets started.
// The .gitignore file is used to specify which files and directories to ignore when pushing code to a Git repository.
// The .watchmanconfig file is used to configure Watchman, a file watching service that is used by the React Native CLI.
// The .flowconfig file is used to configure Flow, a static type checker for JavaScript.
// The .prettierrc file is used to configure Prettier, a code formatter that helps maintain consistent code style.
// The .prettierignore file is used to specify which files and directories to ignore when formatting code with Prettier.
// The .buckconfig file is used to configure Buck, a build system developed by Facebook.
// The .buckconfig.local file is used to configure Buck for local development.


// Package.json file contains all the information about the project while Package-lock.json file contains the information about the dependencies of the project.

// The watchman file will only detect the changes made in the projet while metro file will implement and reload the file.
